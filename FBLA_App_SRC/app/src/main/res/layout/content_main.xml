<?xml version="1.0" encoding="utf-8"?>
<!--This initiates the overall layout of the page -->
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:showIn="@layout/activity_main"
    tools:context="fbla.com.fbla_app_src.MainActivity"
    android:background="@drawable/main_bckg">
    <!--This section of code represent the title image for the application   -->
    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/logo"
        android:src="@drawable/whitewithtagline"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="50dp"
        />
    <!--This section of code takes the user to the sign up page  -->
    <Button
        android:layout_marginTop="20dp"
        android:layout_width="300dp"
        android:layout_height="40dp"
        android:text="Sign up "
        android:id="@+id/signupEmailButton"
        android:layout_below="@+id/logo"
        android:background="@drawable/mybutton"
        android:layout_centerHorizontal="true"
        android:textColor="#ffffff"
        android:textAppearance="@style/TextAppearance.FontPath"

        />
    <!--This section of code represent the text that asks the user if they already have an account  -->
    <TextView
        android:layout_width="wrap_content"
        android:textAppearance="@style/TextAppearance.FontPath"
        android:layout_height="wrap_content"
        android:text="Already have an account?"
        android:id="@+id/account"
        android:textSize="20dp"
        android:textColor="#ffffff"
        android:layout_marginTop="10dp"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:layout_below="@+id/signupEmailButton" />
    <!--This section of code takes the user to the login page if they already have an account  -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Sign in here."
        android:textAppearance="@style/TextAppearance.FontPath"
        android:id="@+id/main_SignInEmail"
        android:textColor="#60C6C7"
        android:layout_below="@+id/account"
        android:layout_centerHorizontal="true" />
</RelativeLayout>
